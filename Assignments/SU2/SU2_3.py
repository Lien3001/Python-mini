# Karlien Heleen Brink, 52444015

print('ASCII and Character conversion\n')

character = input('Enter character: ')  # Get alphabetical character
print('The ASCII value is:', ord(character), '\n')  # Convert to ASCII/print

character_value = int(input('Enter the ASCII value: '))  # Get number
print('The Character is:', chr(character_value))
